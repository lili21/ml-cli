#!/usr/bin/env node

var program = require('commander');

program
  .version(require('../package.json').version)
  .usage('<command>')
  .command('init <project-name>', 'generate a new project')
  .parse(process.argv);
